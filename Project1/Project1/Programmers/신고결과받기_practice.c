#include <stdio.h>
#include <stdbool.h>
#include <stdlib.h>
#include <string.h>

int arr[1000][1000];
int cnt[1000];

int solution(const char* report[]);

int main()
{
	const char* _report[3]={"Hello", "My", "PC"};
	solution(_report[3]);
	return 0;
}

int solution(const char* report[])
{
//	const char* text[3];
//	for (int i = 0; i<3; i++)
//	{
//		strcpy(text, report[i]);
//	}
	return 0;
}


//int* solution(const char* id_list[], size_t id_list_len, const char* report[], size_t report_len, int k) {
//    // return 값은 malloc 등 동적 할당을 사용해주세요. 할당 길이는 상황에 맞게 변경해주세요.
//    int* answer = (int*)calloc(id_list_len,sizeof(int));
//    char* str;
//	char* text[21];
//
//    return answer;
//}
